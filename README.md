# hatchtrack-mqtt-influxdb-bridge

## About

This program listens to a specific MQTT topic for any new messages. When a messages is received, it formats it and writes it to an InfluxDB instance that stores all the data.
